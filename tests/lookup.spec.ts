import { lookup } from "dns";
import { describe } from "node:test";

describe("lookupEvents", () => {
    it("fetches events based on lookup criteria", async () => {
        const events = await lookup
    }